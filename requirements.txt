# NexusMD — Python dependencies for Railway deployment

fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.7.1
python-multipart==0.0.9
aiofiles==23.2.1

# Job queue (Redis optional — in-memory fallback built in)
redis==5.0.4

# RDKit for scaffold/Tanimoto search (unpinned — gets latest stable)
rdkit
rdkit
meeko
gemmi
