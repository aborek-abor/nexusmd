# NexusMD — Python dependencies
# Railway deployment (no Redis required — falls back to in-memory queue)

fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.7.1
python-multipart==0.0.9

# Static file serving (frontend)
aiofiles==23.2.1

# Job queue (Redis optional — in-memory fallback works fine)
redis==5.0.4

# RDKit for real scaffold/Tanimoto search
rdkit==2024.3.1
