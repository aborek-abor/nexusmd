# NexusMD — Python dependencies for Railway deployment

fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.7.1
python-multipart==0.0.9
aiofiles==23.2.1

# Job queue (Redis optional — in-memory fallback built in)
redis==5.0.4

# RDKit for scaffold/Tanimoto search + SMILES→3D fallback when OBabel unavailable
rdkit

# S3-compatible bucket storage (Railway Bucket / AWS S3)
boto3==1.34.69

# Molecular Dynamics — OpenMM ecosystem
openmm==8.1.1
pdbfixer==1.9
mdtraj==1.9.7
